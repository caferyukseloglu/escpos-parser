const CommandTwoArgs = require('./CommandTwoArgs')

class SetRelativeVerticalPrintPositionCmd extends CommandTwoArgs {
  // You can add specific implementation for this command if needed
}

module.exports = SetRelativeVerticalPrintPositionCmd
