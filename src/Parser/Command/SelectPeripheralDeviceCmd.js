const CommandOneArg = require('./CommandOneArg')

class SelectPeripheralDeviceCmd extends CommandOneArg {
  // Add any specific logic for SelectPeripheralDeviceCmd if needed
}

module.exports = SelectPeripheralDeviceCmd
